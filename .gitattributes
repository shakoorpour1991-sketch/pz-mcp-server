# Normalize line endings to LF in the repo and on every checkout so
# `npm run format:check` (prettier, endOfLine=lf) is deterministic on
# windows-latest CI runners and for Windows local contributors.
* text=auto eol=lf
